# dependencies
/node_modules
/.pnp
.pnp.js

# production
/.next
/out

# env files — never commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# misc
.DS_Store
*.pem
npm-debug.log*

# raw source photo library — not a build asset, huge, never commit
/site-main-photos
